# Branch map: `main` vs `Replit`

**Repository:** https://github.com/ravenslight2010/Production-run-calculator  
**Compare UI:** https://github.com/ravenslight2010/Production-run-calculator/compare/main...Replit  
**Captured:** 2026-09-20  

| | **main** | **Replit** |
|--|----------|------------|
| **Tip (at capture)** | `8885c9a5` — Merge PR #65 (battery/performance audit) | `3840f5d3` — Merge PR #64 (TS boundary + sync SSE test) |
| **Relationship** | **Diverged** | **121 commits ahead** of merge-base toward Replit · **5 commits behind** main |
| **Files in `main...Replit`** | — | **300** files (`131` modified, `168` added, `1` removed) |

---

## What main has that Replit does not (5 commits)

Mostly **docs/memory + small API/test fixes**, not the big live-tab extraction:

| Theme | Contents |
|-------|----------|
| **API / sync** | Restore API route mounts and heartbeat contracts (**PR #62**); tiny `sync.ts` / integration test / `coreSyncRuns` tweaks |
| **Data heals** | Source-library reconciliation repair + fingerprint manifest updates |
| **Docs (main-only research)** | Battery/performance deep dive + corrections (**PR #65**); sync/importer/auto-track research (**PR #63**); AI system, incident notifications, merge-suggest research; capability/further/improvement packs also landed on main’s memory/docs |
| **E2E** | Small `pwa-handoff.spec.ts` change |

**main-only commits (newest first):**

1. `8885c9a5` — Merge PR #65: correct battery/performance audit priorities  
2. `8768ec88` — docs: correct battery audit claims  
3. `a796abe2` — docs: battery/performance deep dive — timer consolidation is the real finding  
4. `f9e1cf85` — docs: sync system, importer redesign, and auto-track coordination research (#63)  
5. `860d99ea` — Restore API route mounts and heartbeat contracts (#62)  

**main-only file touch list (summary):**

- `.agents/memory/*` — AI system, autotrack coordination, battery performance, capability pack, further research, idea backlog, importer redesign, improvement research, incident notifications, merge-suggest, sync-system improvements, MEMORY index  
- `docs/*` — parallel copies of much of the above + SSE schedule lease refresh design spec  
- `artifacts/api-server` — `dataHeals*`, repair fingerprints, source-library reconciliation repair, `coreSyncRuns`, `sync.ts`, `sync.integration.test.ts`  
- `artifacts/run-calculator/e2e/pwa-handoff.spec.ts`  

So main is **not** empty of research—it picked up a parallel docs stream and a **route-mount/heartbeat** fix that Replit may still need when merging.

---

## What Replit has that main does not (~121 commits)

Rough theme breakdown from commit messages (many are Replit “Published your App” / “Git commit prior to merge” noise):

| Theme | ~Count | What it is |
|-------|--------|------------|
| **Merge/publish noise** | ~29 | Agent/Replit checkpoint commits |
| **Other / mixed** | ~38 | Heals, CI evidence, Replit config, merges from main, assorted |
| **Docs / research / planning** | ~17 | Idea backlog, capability pack, sync/reconnect/server deep dives, agent metadata |
| **Wake / sync recovery** | ~12 | Faster wake, diagnostics in sync activity, day-break convergence tests, SSE lease refresh, **delta sync to peer SSE**, packaging corrections across wake |
| **Tests / soaks** | ~11 | Peer sync soak, lifecycle race convergence, live-station startup smoke, release Node launcher tests |
| **Live-tab extraction** | ~6 | Split live UI out of `home.tsx` into `live-stations/*` + `liveTabsSupport.tsx` |
| **Sauce / packaging / staged supply** | ~6 | Correction locks, manual counts, warehouse switchover banner, staged supply, pause packaging under server authority |
| **Timeline / breaks** | ~1 | Whole-day timeline + fixed breaks |
| **Server jobs / background** | ~1–2 | Job enhancements + backoff docs |

### Sample commits by theme

**Wake / sync recovery**

- Speed up visible wake sync recovery  
- Add bounded wake recovery timing diagnostics  
- Show wake-recovery details in sync activity  
- Add sync integration coverage for day break convergence  
- Preserve packaging corrections across wake reconciliation  
- Replace wake rebase boolean with explicit policy reasons  
- Extend delta sync to peer SSE clients  
- Refresh auto-track schedule leases over SSE  
- fix/Merge PR #64: TypeScript boundary and flaky sync SSE retry budget  

**Live-tab extraction**

- Extract remaining live tabs into focused modules  
- Guard live tab support from importing Home  
- Parse live-tab boundary imports instead of scanning source text  
- Restore live-tab run-value stamp protection  
- Add live-tab persistence and stale-peer regression coverage  
- Guard extracted live tabs against copied run-value persistence imports  

**Sauce / packaging / staged supply**

- Cover packaging and sauce manual counts across remounts and stale peers  
- Cover Sauce and Frontline correction locks during peer edits  
- Remove legacy Sauce barrel alerts and preserve canonical staged supply  
- Automate staged Sauce and Frontline supply  
- Keep paused Packaging counting through server authority  
- Move the press-based switchover alert from Run to Warehouse  

**Timeline**

- Add synced whole-day timeline and fixed break scheduling  

**Tests / soaks**

- Add deterministic full-shift peer sync soak coverage  
- Test peer sync savings across run lifecycle transitions  
- Test concurrent lifecycle conflict convergence  
- Test Pause/Resume lifecycle race convergence  
- Add real-context startup smoke tests for live stations  
- Make run-detail substitution coverage behavioral and today-scoped  
- Release Node launcher / retained evaluation discovery tests  

---

## Diff by area (`main...Replit`)

| Area | Files | Line churn (approx) | Notes |
|------|------:|---------------------|--------|
| **run-calculator (web)** | 82 | **+16k / −6.8k** | Dominant product delta |
| **api-server** | 29 | **+2.3k / −0.1k** | Sync route, auto-track ticks, background ops, big integration tests |
| **attached_assets** | 14 | **+2.4k** | Uploaded research markdown copies |
| **.agents** | 18 | +0.4k / −0.1k | Memory notes, skills |
| **docs** | 22 | often 0-line in API | Present on branch; many show 0±0 in compare (mode/rename/identical blob quirks) |
| **research/** | 50 | 0±0 in API | Full `research/` tree on Replit (sources, causality, pool, SSE, etc.) |
| **lib/** (live-calc, sync-contract, api-zod, …) | many | mixed / 0 | Codegen and shared libs touched |
| **release-evidence / scripts / .github** | smaller | mixed | Launcher/evidence/CI alignment |

**File status counts:** 131 modified · 168 added · 1 removed · ~159 files reported with 0±0 line change in the GitHub compare payload (docs/research/assets often in this set).

---

## Largest code deltas (Replit ahead)

### Web — live station extraction (biggest structural change)

- `artifacts/run-calculator/src/pages/home.tsx` — huge net **deletion** (~−6.3k lines) as UI moves out  
- **New modules (examples):**
  - `pages/liveTabsSupport.tsx` (~+2.5k)
  - `components/live-stations/LiveRunTabContent.tsx` (~+2.3k)
  - `LiveSummaryTabContent.tsx` (~+1.8k)
  - `LiveSetupRecipesTabContent.tsx` (~+1.5k)
  - `LiveStoppagesTabContent.tsx` (~+1.0k)
  - `LiveDoughTabContent.tsx` (~+1.0k)
  - `LivePackagingTabContent.tsx` (~+0.7k)
  - `LiveSauceTabContent.tsx` (~+0.4k)
  - station startup tests  
- Related: run-value stamp protection, stale-peer persistence guards, manual section locks tests  
- `WarehouseSwitchoverBanner` (+ tests)  
- Sync client: `syncWriteResponse`, diagnostics, wake recovery UX  

### API

- `artifacts/api-server/src/routes/sync.ts` (+~270) — wake/delta/SSE-related behavior  
- `sync.integration.test.ts` (**+~1k**) — day-break, peer, lifecycle coverage  
- `autoTrackServerTicks.ts`, `backgroundOperations`, sync contract tests  

### Product features on Replit (not just refactors)

- Synced **day timeline + fixed breaks**  
- **Sauce/frontline staged supply** + correction locks  
- **Packaging** manual counts / pause under server authority  
- **Wake recovery** speed + timing diagnostics in sync activity  
- Commit trail also mentions **delta sync extended to peer SSE** and **auto-track schedule leases over SSE** (verify carefully in merge review)

---

## Mental model

```text
                    merge-base
                   /          \
              main (5)      Replit (121)
                 |               |
    route mounts #62      live-tab extraction
    battery docs #65      wake/sync hardening
    importer/sync docs    sauce/packaging/timeline
    small heal/sync tests  research/ + attached docs
                           large sync integration tests
```

- **Replit** = floor UX extraction + operational sync/auto-track hardening + research dump + noisy agent commits.  
- **main** = narrower set of **integration fixes** (route mounts/heartbeat) + **battery/performance** and other **docs** that Replit may lack or only partially share.

---

## Merge implications

1. **Not a fast-forward** either way — need merge or rebase; expect conflicts in `home.tsx` / sync / memory index.  
2. Bring **PR #62** (route mounts + heartbeat) onto Replit before relying on API contracts.  
3. Reconcile **docs**: both sides have research packs; prefer one tree (`docs/` + `.agents/memory`) and drop duplicate `attached_assets` copies if desired.  
4. Treat Replit **live-tab extraction** as the main review surface for product behavior.  
5. Re-run **sync SSE + integration shards** after merge (both sides touched them).  
6. Verify claimed behaviors from commit messages (peer SSE delta frames, schedule leases) against the actual `sync.ts` / client code during review.

---

## Suggested merge checklist

### Must take from main → into Replit (or into merge result)

- [ ] PR #62 route mounts + heartbeat contracts (`sync.ts`, related capability mounts, integration tests)  
- [ ] Source-library reconciliation repair / fingerprint updates if still applicable  
- [ ] Battery/performance research docs (or consciously drop if superseded)  
- [ ] Any main-only memory notes not duplicated on Replit  

### Must keep from Replit → into merge result

- [ ] Live-stations extraction + `liveTabsSupport`  
- [ ] Wake recovery diagnostics + day-break / peer soak tests  
- [ ] Sauce/frontline/packaging correction and staged-supply behavior  
- [ ] Day timeline + fixed breaks  
- [ ] Expanded `sync.integration.test.ts` coverage  
- [ ] `research/` tree if you want the graded deep-dives in-repo  

### Conflict hotspots (review first)

- [ ] `artifacts/run-calculator/src/pages/home.tsx`  
- [ ] `artifacts/api-server/src/routes/sync.ts`  
- [ ] `artifacts/api-server/src/routes/sync.integration.test.ts`  
- [ ] `.agents/memory/MEMORY.md` / idea-backlog / overlapping research docs  
- [ ] Generated `lib/api-zod` / client packages if both sides regenerated  

---

## Related local research corpus (this workspace)

These were written during earlier research sessions under `docs-update/` (not necessarily committed to either branch):

- `capability-research-pack-2026-09-18.md`
- `improvement-research-2026-09-18.md` / `further-research-2026-09-18.md`
- `server-research-2026-09-19.md` / `server-research-deep-dive-2026-09-19.md`
- `sync-deep-dive-2026-09-19.md` / `reconnect-reliability-deep-dive-2026-09-19.md`
- `deep-dive-research-continuation-2026-09-19.md`
- `domain-deep-dives-2026-09-19.md` / `all-remaining-deep-dives-2026-09-19.md`
- `pre-implementation-research-pack-2026-09-19.md`
- **This file:** `main-vs-replit-branch-map-2026-09-20.md`

---

*Snapshot only—re-run `gh compare main...Replit` (or the GitHub compare URL) before merging; tips move.*
